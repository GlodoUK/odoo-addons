<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <template id="tickets_followup" inherit_id="helpdesk.tickets_followup">
        <xpath expr="//div[@id='ticket-nav']" position="before">
            <div t-if="ticket.team_id.allow_portal_ticket_reopen and ticket.stage_id.fold" class="flex-grow-1">
                <div class="d-grid flex-sm-nowrap pt-2">
                    <button class="btn btn-light pt-1" data-bs-target="#helpdesk_ticket_reopen_modal" data-bs-toggle="modal">
                        Reopen Ticket
                    </button>
                </div>
            </div>

            <div class="modal" tabindex="-1" role="dialog" id="helpdesk_ticket_reopen_modal">
                <div class="modal-dialog mt-5" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title">Reopen Ticket</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"/>
                        </div>
                        <div class="modal-body">
                            <p>Are you sure you wish to proceed?</p>
                        </div>
                        <div class="modal-footer">
                            <a role="button" class="btn btn-primary" t-att-href="'/my/ticket/reopen/%s/%s' % (ticket.id, ticket.access_token)">Reopen Ticket</a>
                            <button type="button" class="btn btn-light" data-bs-dismiss="modal">Close</button>
                        </div>
                    </div>
                </div>
            </div>
        </xpath>
    </template>
</odoo>

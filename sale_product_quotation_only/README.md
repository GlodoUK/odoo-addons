# Sale Product Quotation Only

## Overview

Allows product templates to be marked as "Quotation Only", preventing them from being
included on confirmed sale orders.

## Usage

1. Navigate to a product template form (Sales > Products > Products)
2. Enable the "Quotation Only" checkbox on the product
3. The product can now be added to draft quotations but cannot be on a confirmed sale
   order

<odoo>
    <!--    ticket number on breadcrumbs, xpath acting out here, needs a fix if it gets into conflicts with other modules-->
    <template
        id="helpdesk.portal_my_home_menu_helpdesk"
        name="Portal layout : helpdesk tickets menu entries"
        inherit_id="portal.portal_breadcrumbs"
        priority="50"
    >
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <t t-set="rating_page" t-value="page_name == 'rating'" />
            <li
                t-if="page_name == 'ticket' or ticket or rating_page"
                t-attf-class="breadcrumb-item #{'active ' if not ticket else ''}"
            >
                <a
                    t-if="ticket or rating_page"
                    t-attf-href="/my/tickets?{{ keep_query() }}"
                >Tickets</a>
                <t t-else="">Tickets</t>
            </li>
            <li t-if="ticket" class="breadcrumb-item active">
                <span t-field="ticket.name" /> (<span t-field="ticket.number" />)
            </li>
            <li t-if="rating_page" t-attf-class="breadcrumb-item active">
                Our Ratings
            </li>
        </xpath>
    </template>
<!--    Ticket name with number in ticket portal table-->
    <template
        id="helpdesk_team_ticket_sequence_portal_helpdesk_ticket"
        name="Helpdesk Tickets Name With Number On Ticket Table Page"
        inherit_id="helpdesk.portal_helpdesk_ticket"
        priority="99"
    >
        <xpath
            expr="//t/t/t/t/t/tr/td/a/small[contains(text(),'#')]/../t"
            position="replace"
        >
            <t t-esc="ticket.number" />
        </xpath>
        <xpath
            expr="//t/t/t/t/t/tr/td/a/small[contains(text(),'#')]"
            position="replace"
        />
    </template>
    <!--    Ticket name on portal ticket page-->
    <template
        id="helpdesk_team_ticket_sequence_tickets_followup"
        name="Helpdesk Tickets Name With Number On Ticket Page"
        inherit_id="helpdesk.tickets_followup"
        priority="99"
    >
        <xpath
            expr="//t/t/t/div/div/h5/div/small[hasclass('text-muted')]"
            position="replace"
        >
            <small class="text-muted "> (<span t-field="ticket.number" />)</small>
        </xpath>
    </template>
</odoo>

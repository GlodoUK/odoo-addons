<?xml version='1.0' encoding='utf-8' ?>
<odoo>
    <record id="view_order_form" model="ir.ui.view">
        <field name="name">sale.order.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="priority">75</field>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button
                    class="oe_stat_button"
                    type="object"
                    name="action_view_ticket_ids"
                    groups="helpdesk.group_helpdesk_user"
                    icon="fa-ticket"
                >
                    <field
                        string="Tickets"
                        name="helpdesk_tickets_count"
                        widget="statinfo"
                    />
                </button>
            </xpath>

            <xpath expr="//group[@name='sale_reporting']" position="after">
                <group string="Helpdesk">
                    <field name="helpdesk_tickets_ids" widget="many2many_tags" />
                </group>
            </xpath>
        </field>
    </record>
</odoo>

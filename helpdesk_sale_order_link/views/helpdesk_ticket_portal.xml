<odoo>
    <template
        id="helpdesk_ticket_sale_order_link_portal"
        name="Portal: Helpdesk Tickets with Sale Order link"
        inherit_id="helpdesk.tickets_followup"
        priority="75"
    >
        <xpath expr="//div[@name='description']" position="after">
            <div
                class="row mb-4"
                t-if="ticket.so_ids"
                id="quotations"
                name="Quotations"
            >
                <strong class="col-lg-2">Linked Quotations</strong>
                <div class="col-lg-10">
                    <p t-foreach="ticket.so_ids" t-as="sale">
                        <a t-attf-href="/my/orders/#{sale.id}">
                            <t t-esc="sale.name" />
                        </a>
                    </p>
                </div>
            </div>
        </xpath>
    </template>
    <template
        id="helpdesk_sale_order_ticket_link_portal"
        name="Portal: Sale Order with Helpdesk Ticket link"
        inherit_id="sale.sale_order_portal_content"
        priority="75"
    >
        <xpath expr="//div[@id='informations']" position="inside">
            <div
                class="row mb-4"
                t-if="sale_order.helpdesk_ticket_id"
                id="ticket"
                name="Ticket"
            >
                <strong class="col-lg-2">Linked Helpdesk Ticket</strong>
                <div class="col-lg-10">
                    <p>
                        <a
                            t-attf-href="/helpdesk/ticket/#{sale_order.helpdesk_ticket_id.id}"
                        >
                            <t t-esc="sale_order.helpdesk_ticket_id.description" />
                        </a>
                    </p>
                </div>
            </div>
        </xpath>
    </template>
</odoo>

=================
Partner Hierarchy
=================

Links ``res.partner`` records into an arbitrarily deep parent/child tree that is
**separate from** ``parent_id``.

If you need arbitrary n-to-n typed relations between partners instead, OCA's
``partner_multi_relation`` is the module you want.

==================
connector_edi_sale
==================

Integrates `connector_edi` with `sale_stock` and `account` by providing hooks
and relationships and primitives for common "EDI" operations that will be
required for sales and invoicing.

